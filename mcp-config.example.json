{
  "_comment": "Sample workspace-level MCP config. Copy to <your-project>/.mcp.json (Copilot CLI / Claude Code / Cursor all use this same format). Edit absolute paths to match your clone location.",
  "mcpServers": {
    "ssh-remote": {
      "type": "local",
      "command": "/home/agony/TiMidlY-projects/ssh-remote-mcp/.venv/bin/python",
      "args": [
        "/home/agony/TiMidlY-projects/ssh-remote-mcp/server.py"
      ],
      "env": {},
      "tools": ["*"]
    }
  }
}
