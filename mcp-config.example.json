{
  "_comment": "Sample workspace-level MCP config. Copy to <your-project>/.mcp.json (Copilot CLI / Claude Code / Cursor all use this same format). Uses uvx so no clone or venv is required.",
  "mcpServers": {
    "portal": {
      "type": "local",
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/TMYTiMidlY/portal-mcp-server.git",
        "portal-mcp-server"
      ],
      "env": {},
      "tools": ["*"]
    }
  }
}
